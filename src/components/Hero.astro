---
const { title, subtitle, ctaText } = Astro.props;
---
<section class="bg-white text-center py-20 px-6 shadow-md">
  <h1 class="text-4xl sm:text-6xl font-bold text-gray-900">{title}</h1>
  <p class="mt-4 text-lg text-gray-600 max-w-xl mx-auto">{subtitle}</p>
  <a href="#get-started" class="inline-block mt-6 bg-blue-600 text-white text-lg px-6 py-3 rounded-full hover:bg-blue-700 transition">
    {ctaText}
  </a>
</section>
